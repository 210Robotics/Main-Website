import type { Metadata } from "next";
import { CTA, MemberCard, PageHero, SectionHeading } from "@/components/ui";
import { members } from "@/lib/site-data";
export const metadata:Metadata={title:"Team"};
export default function Team(){const officers=members.filter((member)=>member.role!=="Member");const contributors=members.filter((member)=>member.projects.includes("SIDC"));return <><PageHero eyebrow="The people of 210" title="Every machine is a team effort." body="Officers set organization-wide direction. Members bring the curiosity and craft that make every system work." image="/media/brand/siemens-team.png"/><section className="section"><div className="shell"><SectionHeading eyebrow="Organization leadership" title="Built and led by students."/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{officers.map((member)=><MemberCard key={member.id} member={member}/>)}</div></div></section><section className="section border-y border-[#282828] bg-[#0d0d0d]"><div className="shell"><SectionHeading eyebrow="Winning project" title="RoboRowdy contributors." body="Andrew Romo and every verified contributor are included through project assignments without adding branch labels to their public titles."/><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{contributors.map((member)=><MemberCard key={member.id} member={member}/>)}</div></div></section><CTA/></>}
