import Image from "next/image";

export function SplitFeature({eyebrow,title,body,image,reverse=false,children}:{eyebrow:string;title:string;body:string;image:string;reverse?:boolean;children?:React.ReactNode}){return <section className="section"><div className={`shell grid items-center gap-12 lg:grid-cols-2 ${reverse?"lg:[&>*:first-child]:order-2":""}`}><div><p className="eyebrow">{eyebrow}</p><h2 className="headline">{title}</h2><p className="lede mt-6">{body}</p>{children}</div><div className="relative h-[420px] overflow-hidden border border-[#333]"><Image src={image} alt="" fill sizes="(max-width:1024px) 100vw, 50vw" className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-black/55 to-transparent"/></div></div></section>}

export function NumberedList({items}:{items:{title:string;body:string}[]}){return <div className="mt-9 divide-y divide-[#333] border-y border-[#333]">{items.map((item,i)=><div className="grid gap-3 py-5 sm:grid-cols-[52px_1fr]" key={`${i}-${item.title}`}><span className="font-mono text-sm text-[#fd7803]">0{i+1}</span><div><h3 className="font-bold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-[#888]">{item.body}</p></div></div>)}</div>}

export function Metrics({items}:{items:{value:string;label:string}[]}){return <section className="border-y border-[#292929] bg-[#0e0e0e]"><div className="shell grid sm:grid-cols-2 lg:grid-cols-4">{items.map(item=><div key={item.label} className="border-b border-[#292929] py-8 sm:border-r sm:px-7"><strong className="text-4xl text-[#fd7803]">{item.value}</strong><p className="mt-2 font-mono text-[.65rem] uppercase tracking-wider text-[#999]">{item.label}</p></div>)}</div></section>}
